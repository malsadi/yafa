# Dependencies and build output
node_modules/
dist/
.wrangler/

# Generated by `wrangler types` (T-011); regenerated by the typecheck script.
worker-configuration.d.ts

# Secrets (brief section 12): never in the repository
.dev.vars
.dev.vars.*
.env
.env.*
# ...except the names-only template (T-011), which must be committed for CI.
!.dev.vars.example

# Test and tool output
coverage/
playwright-report/
test-results/
*.log

# Local editor and Claude Code settings
.claude/settings.local.json
.DS_Store
